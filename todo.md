# Project TODO

> **現行版本：** 固定共用帳本「歲時錄 · Vellum Tides」，掌簿為玉瑟與石琴，採 V2 的流向／大目／名目／歲時／符契詞彙。下列只顯示尚待完成或剛完成的現行工作；早期完成歷程保留於檔尾的隱藏註解，避免誤導日常維護。

- [x] 實作帳頁翻閱的伺服器端篩選、45 筆分段載入、月份收闔與日期框組，避免長期使用後一次渲染全冊資料。
- [x] 實作完整 JSON 帳本典藏、格式與固定帳本核對、匯入前預覽、離線／待補登保護及僅追加式重複防護。
- [x] 使用者已在 Supabase SQL Editor 成功套用 `歲時錄-v2-帳頁分段與備份.sql`，完成一筆離線新增後恢復連線補登，並成功匯出 JSON 帳本備份。
- [x] 使用者已確認讀入備份後的備份快照摘要正常顯示；安全補入的已收起帳頁復原與重複略過亦已完成實測。
- [x] 縮小「添一頁新帳」與「憑據入冊」的歲時日期欄位高度，避免原生日期文字在欄位內顯得過大或破壞雙欄對齊。
- [x] 修正帳頁翻閱的篩選日期欄位最小寬度與欄距，避免雙欄在中等螢幕互相擠壓或重疊。
- [x] 精簡月冊標題與帳頁卡片的文字密度，讓長名目以單行省略顯示而非跨行推高卡片；保留 `title` 與 `aria-label` 的完整內容標示。
- [x] 完成未使用檔案與歷史 SQL 的安全清理，僅保留現行 V2 建置、固定共用帳本、核對及帳頁分段／備份腳本。
- [x] 完成現行文件與待辦整編；型別檢查、31 項測試與建置已通過，且桌面 `1280 × 720`、行動 `375 × 812` 的封閉登入介面均可正常載入與閱讀。
- [x] 日期與文字密度調整後，再次於桌面 `1280 × 720` 與行動 `375 × 812` 核對公開登入入口，皆無橫向溢出、文字遮蔽或按鈕失序；私密帳頁需由玉瑟或石琴登入後檢視實際帳務資料。
- [x] 將添新帳、憑據入冊及帳頁篩選的歲時欄位改為可隨內容收縮的日期控制項，不再在雙欄表單中硬撐為等寬的大型欄位。
- [x] 重新編排帳頁翻閱卡片，令名目、符契、大目、掌簿與金額各自對齊並減少空白，長名目在可用寬度內保持可讀且不推擠金額。
- [x] 在憑據入冊提供直接開啟裝置相機拍攝的入口，並保留選擇既有照片的替代方式。
- [x] 將憑據照片 OCR 結果預先帶入逐項名目、金額、固定大目與符契建議；使用者只需校對、修正與確認歸帳。QR／Barcode 文字入口維持手動補寫品項的輔助方式。
- [x] 完成典藏規則的程式與文件調整：完整備份改為只收錄有效帳頁，且保留安全僅追加式還原。
- [x] 完成帳頁永久刪除的前端雙重確認與 Supabase SQL 函式實作。
- [x] 完成「還原至備份快照」的前端雙重確認、Supabase SQL 函式與操作說明實作。
- [x] 將添一頁新帳的歲時欄位改為與名目欄同寬；將憑據眉批的商店、發票號碼、歲時與隨機碼統一為商店欄寬。
- [x] 在憑據入冊加入 QR Code 相機掃描入口，保留手動貼入文字作為替代方式，並將辨識文字帶入既有 QR／Barcode 解析流程。
- [x] 將帳頁翻閱卡片的掌簿資訊移至大目與符契之後，維持名目與金額的緊湊對齊。
- [x] 查明並修正介面定時重新整理後跳回流向頁的問題，確保使用者停留的頁籤在資料重新載入後仍被保留。
- [x] 新增並通過有效帳頁數的回歸稽核：完整備份 SQL 僅匯出未收起帳頁，摘要則可正確區分帳頁總數與有效帳頁數；使用者毋須在日常操作中另行核對。
- [x] 以實際資料驗證永久清除：建立並收起帳頁後，執行「永久清除已收起帳頁」，確認帳頁與相關資料均被清除且不可逆。
- [x] 使用者已完成快照還原實測，確認新增資料可移除、收起資料可補回，且後續修改可覆寫回備份值。
- [x] 修正使用者實測「還原至備份快照」執行後無任何資料變更的問題，並讓完成提示呈現可讀的結果摘要。
- [x] 完成安全補入的已收起帳頁復原程式與 SQL 修正：只重新啟用備份內已收起的同識別碼帳頁，不覆寫仍有效帳頁。
- [x] 使用者重新套用 `歲時錄-v2-典藏快照與永久刪除.sql` 後，已回報安全補入以「備份 3、4 → 收起 3、4 → 新增 5、6」情境實測正常。
- [x] 快照還原的實際操作結果已由使用者確認正常；不再要求額外抄錄完成提示文字或筆數。
- [x] 使用者以「新增 1、2、3、4 → 收起 1、2 → 備份 → 新增 5、6 → 收起 3、4 → 新增 7、8 → 安全補入」實測：提示顯示復原 2 筆、其他 0 筆，帳頁保留 5、6、3、4、7、8，符合安全補入不移除備份後有效帳頁的定義。
- [x] 使用者續測將 1 至 8 全數收起並永久清除，介面正確顯示清除 8 頁；再用先前備份安全補入後只出現備份內有效的 3、4，符合永久清除後可由備份重建的預期。
- [x] 檢診全站配色是否以羊皮紙白、墨藍、苔綠、赭金及其深淺延伸為主，並盤點文字是否維持古典文雅；使用者已確認修訂清單並同意實作。
- [x] 建立羊皮紙白、墨藍、苔綠、赭金四色的語意令牌，將元件中的直接色碼、散逸色、警示色與圖表色收束為同一色系。
- [x] 重整登入入口、出入摘要、憑據表單與典藏操作的材質、卡片與按鈕層級，使其採用一致的簿冊紙面與墨色語言。
- [x] 將流程小標、觀測標籤與操作文案全面收束為古典文雅語氣，同時保留不可逆操作的明確性。
- [x] 使用者已登入確認私密頁面顏色呈現正常，並回報桌面與行動尺寸的對比、溢出與閱讀性均正常。
- [x] 將登入入口的電子郵件、密語與兩段引言改為指定古典稱謂，並刪除「玉瑟與石琴，共讀這一冊。」副標。
- [x] 將 `shared/ledger.ts` 的各大目 `tone` 全數改為 vellum／ink／moss／ochre 延伸色，避免灰藍與紫色殘留於圖表。
- [x] 將 `client/src/components/LedgerBrowser.tsx` 內殘留的直接 `#xxxxxx` 色碼改寫為 vellum／ink／moss／ochre 語意令牌或對應工具類。
- [x] 全站搜尋使用者可見英文與非古典區塊文案；保留品牌名「Vellum Tides」及作為下載格式名稱的 CSV，並將其餘可見術語列入替換。
- [x] 將仍留在介面的非必要英文標示統一改寫為既定中文：掃讀入口與備份格式提示均已採簿冊語彙，並補上對應回歸測試；其後已再統一為「直紋條契」「方陣圖印」「鏡觀條印」與「觀圖析字」。
- [x] 將帳頁翻閱的 `LEDGER READING ROOM` 改為「帳頁卷覽」，並確認該元件不再含直接色碼。
- [x] 新增帳頁成功後保留在「開卷添潤」頁面，讓使用者可以連續登記，不再自動跳至帳頁翻閱。
- [x] 修正新增與覆寫帳頁後「帳頁翻閱」沒有立即更新的問題，並使取消覆寫時返回帳頁翻閱而非新帳表單。
- [x] 將收起帳頁的警告收束為一次明確確認，避免重複跳出提示。
- [x] 調整每日帳頁框底部留白，並在帳頁翻閱卡片顯示附註內容且不擠壓金額。
- [x] 將流向摘要的本月入納、散逸、留存合併為並排「當月」資訊，並新增年度彙總的「今歲」資訊。
- [x] 全站導航詞彙改為「開卷添潤」、「出入流轉」與「簿冊規制」，並更新相關按鈕與導覽文字。
- [x] 重新檢視出入流轉各摘要、洞察與圖表卡片，將殘留的非四色背景、邊線、文字與圓環色收束為 vellum／ink／moss／ochre 令牌。
- [x] 將使用者可見的條碼與識讀詞彙統一為「直紋條契」「方陣圖印」「鏡觀條印」與「觀圖析字」，避用指定禁詞。
- [x] 以使用者提供的圓形紙箋圖檔取代登入入口圖標與網站圖標，並於桌面及行動版核對顯示比例、留白與無溢出排列。
- [x] （不採用）先前誤將正式橫式「歲時錄 · Vellum Tides」標誌置換登入入口與網站識別；依使用者釐清後未保留此變更。
- [x] （不採用）先前誤認需要替換登入入口上方圖示；依使用者釐清後已維持該區原有文字與圖示結構。
- [x] 將登入後左側欄頂端「歲時錄」文字左方的藍底葉子圖示改為使用者提供的 `icon.svg`；不改動登入入口圖示，並加入回歸契約。
- [x] 稽核專案檔案，保留程式碼、必要設定、測試、SQL、文件與 GitHub 封裝所需檔案，移除不再使用的暫存、備份與重複殘留。
- [x] 移除未被應用程式引用的平台模板、偵錯資產、空白保留檔與已完成的配色檢診文件，同時保留安裝、建置、測試、Supabase SQL 與專案文件所需內容。
- [x] 產生可直接上傳 GitHub 的專案封裝檔，並附上精簡後的目錄說明與驗證結果。
- [x] 新增備份快照摘要的有效帳頁數回歸測試，確認收起帳頁會列入總數但不列入有效帳頁數。
- [x] 已將帳本典藏的備份摘要、安全補入、快照還原、永久清除與有效帳頁稽核整編為完成紀錄；不影響已確認正常的既有流程。
- [x] 修正憑據入冊「鏡觀條印」僅開啟相機但未產生辨讀結果的程式原因：改用多格式辨讀器、後鏡頭串流與啟動競態保護，並通過型別檢查、41 項回歸測試與正式建置。
- [x] 使用者已以實際裝置確認鏡觀條印運作正常；直紋條契與方陣圖印辨讀結果可帶入條印文字欄，並保留手動貼入替代入口。
- [x] 使用者已以桌面尺寸登入實測出入流轉、開卷添潤、憑據入冊、帳頁卷覽與簿冊規制，確認四色對比、文字可讀性與版面正常。
- [x] 使用者已以行動尺寸登入實測上述私密頁面，確認表單、卡片、側欄／抽屜與圖表沒有橫向溢出、截斷或難以閱讀情形。
- [x] 修復登入頁載入時的 `Unexpected token '<'` 語法錯誤：移除未設定分析端點時的靜態腳本標記，僅在端點與網站識別皆有效時動態載入分析腳本；入口模組回應已確認為 JavaScript，並補上回歸驗證。
- [x] 已定位再次出現的 `Unexpected token '<'` 來源為清理時誤移除的預覽偵錯腳本路徑；已恢復有效 JavaScript 檔案並加入靜態契約。重新載入公開登入頁、檢查載入資源與主控台後皆未再出現語法錯誤。
- [x] 已重新建立最終 GitHub 原始碼壓縮檔，納入登入頁資源載入修復並排除依賴、建置產物與日誌；壓縮檔已通過解壓縮完整性驗證。
- [x] 建立 `.github/workflows/deploy.yml`，在推送至 GitHub 時自動安裝依賴、執行型別檢查、回歸測試與正式建置，並保存建置產物供後續託管平台使用；README 與回歸契約均已更新，並通過型別檢查、43 項測試與正式建置。
- [x] 診斷 GitHub Actions 執行失敗原因為執行環境找不到 `pnpm`；工作流程改為先以 `pnpm/action-setup@v4` 安裝 pnpm 10.4.1，再啟用快取與安裝依賴，並通過型別檢查、43 項測試與正式建置。
- [x] GitHub Actions 套用 pnpm 初始化修正後仍回報找不到 `pnpm`；依使用者重新指定的部署架構，已停止維護 GitHub Actions，改由 Vercel 連接 GitHub 後自動建置。
- [x] 修正 GitHub Actions 日誌揭示的兩項回歸失敗：該流程已由 Vercel 自動建置取代；本平台預覽檔不再納入 Vercel 原始碼封裝。
- [x] 將正式部署架構調整為 GitHub 儲存原始碼、Vercel 託管 React／Express／tRPC 應用程式、Supabase 提供資料庫與身份驗證；已完成 Vercel 設定、Serverless tRPC 入口、可發布品牌資產、Gemini 觀圖析字 API 與環境變數指引。
- [x] 重新整理 GitHub 原始碼封裝，僅保留可由 Vercel 建置與執行的必要檔案，並提供使用者連接 GitHub、Vercel 與 Supabase 的發布步驟；已建立 `vellum-tides-vercel-source.zip` 並核對必要設定檔均已納入。
- [ ] 使用者在 Vercel 設定 Gemini 環境變數後，以正式網址測試「觀圖析字」API；Vercel 建置、公開網站頁面與 Supabase 登入均已由使用者與未登入環境核對正常。
- [x] 使用者確認 Vercel 首次部署成功後，已核對 GitHub `main` 分支不再含 `.github/workflows/` 的舊建置流程，確認不會與 Vercel 雙軌執行。
- [x] 使用者已確認既有 Vercel 專案 `vellum-tides` 的必要環境變數均已設定；不再重複要求設定。
- [x] 引導使用者將既有 Vercel 專案 `vellum-tides` 連接至 GitHub 倉庫；GitHub 倉庫已成功連接，並由 Vercel 接手後續自動建置。
- [x] 使用者已連接 GitHub 倉庫至 Vercel，但連接前的既有提交未自動補建；已推送含 Vercel 部署設定的新提交至 `main`，並成功觸發首次正式部署。
- [x] 處理 Vercel 正式網址目前要求登入 Vercel 的部署保護設定；使用者已關閉 Require Log In，並以未登入環境重新核對公開網址可直接顯示歲時錄登入頁。
- [x] 核對 GitHub `main` 分支的 Vercel 部署內容；公開倉庫已可讀取 `vercel.json` 與 `api/trpc.ts`，確認 Vercel 設定與 Serverless tRPC 入口均已提交。
- [x] 在 Vercel 最新 Deployment 詳情核對 Source branch 與 commit SHA；使用者提供的詳情畫面顯示來源為 `main`、提交為 `54fd82e`、狀態為 Ready Latest，並提供固定 Production 網域 `vellum-tides.vercel.app`；另以該提交固定網址核對 `vercel.json`、`api/trpc.ts` 均存在，紀錄見 `docs/vercel-live-verification.md`。
- [x] 補齊 iPhone 與 Android 的 PWA 安裝中繼資料：已建立多尺寸歲時錄圖標、Web App Manifest、Apple Touch Icon 與可攜設定，並通過型別檢查、43 項回歸測試、正式建置及建置產物輸出核對。
- [ ] 將 PWA 圖標更新推送至 GitHub `main`，觸發 Vercel 自動部署後，於手機移除舊桌面捷徑並重新加入歲時錄，確認 iPhone 與 Android 均顯示指定識別圖標。
- [x] 依使用者要求，已建立僅含桌面圖標修正檔案的最小覆蓋壓縮檔 `vellum-tides-pwa-icon-update.zip`；不包含環境變數、Vercel 設定或其他既有程式檔案。
- [x] 已列出並稽核 `vellum-tides-pwa-icon-update.zip`：實際內容僅為 `client/index.html`、`client/public/site.webmanifest` 與三個 PWA PNG 圖標；未包含 `.env`、`vercel.json`、`api/trpc.ts`、`package.json` 或 `README.md`。
- [ ] 將已完成本機驗證的觀圖析字原生函式修正提交至 GitHub `main` 並確認 Vercel 重新部署：包含無別名依賴的 `/api/ocr`、Supabase 令牌核對、Gemini 呼叫、照片縮放轉 JPEG 與可讀錯誤提示。
- [ ] 以正式網址核對 `/api/ocr` 可進入原生 Vercel 函式，無論輸入是否有效均回傳 JSON，不再出現 `FUNCTION_INVOCATION_FAILED` 或純文字伺服器錯誤。
- [ ] 由使用者於 iOS 與 Android 各實測一次觀圖析字，確認拍攝或選取照片後能預填商店、歲時、總額與逐項名目；若上游辨識失敗，介面必須顯示可讀提示。
- [x] 已隔離 tRPC／Express／路徑別名函式載入鏈，改為無外部程式相依的原生 `/api/ocr` Serverless 函式；新函式的 Node 22 編譯、JSON 錯誤回應單元測試、型別檢查、45 項回歸測試與正式建置均已通過。
- [x] 已在本機加入 Gemini 模型 404 備援：函式依序嘗試 `gemini-2.5-flash` 與 `gemini-2.0-flash`，並在皆不可用時回覆可讀提示；型別檢查、45 項測試與正式建置均通過。
- [ ] 提交模型備援後，保存並核對正式 `GET/POST https://vellum-tides.vercel.app/api/ocr` 與 GitHub `main` 的實際回應證據，確認手機顯示的 404 究竟來自模型上游或正式部署路由。
- [ ] 補強觀圖析字錯誤呈現：明確區分並顯示金鑰無效、模型不存在、配額不足、輸入格式與暫時性服務失敗，並新增前端回歸契約。
- [ ] 使用正式 GitHub `main` 與 `vellum-tides.vercel.app/api/ocr` 的直接回應重新驗證新函式是否存在；使用者已再次以 iOS 複測仍顯示連線未完成，不可再以本機假設推論部署狀態。
- [x] 依使用者要求，完成全專案檔案稽核並建立 `vellum-tides-clean-source.zip`：保留 Vercel、Supabase、PWA、觀圖析字與必要測試設定；已移除舊 tRPC／Express／Drizzle／Manus 模板伺服器、預覽偵錯資產、研究文件、建置產物、快取、環境檔與未使用元件。型別檢查、44 項離線回歸測試與正式建置均通過，壓縮檔完整性已核對。
- [x] 已重新啟動本機開發伺服器並確認預覽可重新載入：Vite 改為監聽所有網路介面，且已移除清理後殘留的 `tw-animate-css` 匯入；登入入口畫面核對正常。
- [x] 已以 GitHub `main` 提交 `daaf5bafe49d84a6f4b57e5a22309ade8e04b7f7` 的乾淨副本重現 Vercel 錯誤：`client/src/index.css` 殘留已移除套件 `tw-animate-css` 的匯入，導致 Vite 無法解析；本機已移除該匯入並以鎖定依賴建置通過。
- [ ] 將已修正的 `client/src/index.css`（或更新後的完整乾淨原始碼包）提交 GitHub `main`，確認 Vercel 以 `pnpm install --frozen-lockfile && pnpm build` 成功建置。
- [x] 已直接核對 GitHub `main` 與正式網址：`api/ocr.ts` 已存在，`GET /api/ocr` 正確回覆 405 JSON「只接受 POST 請求。」；手機畫面的 404 不再屬於函式未建立的路由問題，待部署模型備援後以 POST 請求取得正式上游證據。
- [x] 已在本機加入 Gemini 模型備援：函式依序嘗試 `gemini-2.5-flash` 與 `gemini-2.0-flash`，並在皆不可用時回覆可讀提示；型別檢查、47 項測試與正式建置均通過。
- [x] 已補強觀圖析字錯誤呈現：原生函式回傳穩定錯誤代碼，行動端明確區分登入、輸入、服務設定、模型不可用、權限、配額、上游暫停與結果不完整。
- [ ] 提交模型備援後，保存並核對正式 `GET/POST https://vellum-tides.vercel.app/api/ocr` 與 GitHub `main` 的實際回應證據，確認模型備援已在正式站運作。
- [x] 修正 Vite 對 `5173-i5mhl4gp6gqoi32rrl979-7cd62328.sg1.manus.computer` 的預覽主機封鎖，重新啟動並確認可存取；型別檢查、47 項測試與正式建置均通過。
- [x] 診斷預覽環境的「觀圖析字未完成」提示：根因為 Vite 無法直接執行 Vercel 原生函式，`/api/ocr` 原先落入單頁應用程式回應；已將本機請求轉送至既有正式原生函式，並以 401 結構化 `AUTH_EXPIRED` JSON 回應、型別檢查、48 項測試與正式建置確認修正。
- [ ] 以手機正式網址 `https://vellum-tides.vercel.app/` 為準追查「觀圖析字未完成」：確認 Production 部署是否已包含前端錯誤分類與 Gemini 備援版本，並蒐集可匿名驗證的正式端點回應，不再以本機預覽結果推論。
- [x] 修正正式站手機觀圖析字的 `Load failed`：依 Vercel 4.5 MB 請求本文限制，將 JPEG data URL 安全上限降至 1,800,000 字元，函式端重複拒絕過大承載；前端對暫時網路中斷自動重試一次，函式端對 Gemini 設 20 秒逾時並回覆結構化錯誤。型別檢查、50 項測試與正式建置均通過；仍待部署後以手機實測。

<!-- 已完成或已淘汰的歷程追溯；不屬於現行維護清單。

- [x] 建立羊皮紙白、墨藍、苔綠與赭金構成的全站設計權杖，並以襯線標題字體統一視覺語言。
- [x] 將應用程式名稱統一修正為「歲時錄 · Vellum Tides」，包含標題、文件、套件與部署資訊。
- [x] 將所有使用者可見的功能性詞彙統一為「入納」、「散逸」、「帳頁」、「流向」與「待確認」，避免一般白話替代詞。
- [x] 設定 Supabase 專案環境變數、用戶端與伺服器端存取層，且不得把服務金鑰傳至瀏覽器。
- [x] 使用新版 `VITE_SUPABASE_PUBLISHABLE_KEY` 設定前端連線，並只在舊版專案相容情況使用 anon public key。
- [x] 撰寫 Supabase SQL 結構，包含雙人帳本、成員、分類、帳頁、發票、發票品項、關鍵字規則與同步事件。
- [x] 建立 Row Level Security 規則，使每個帳本最多兩位成員且只能讀寫自己所屬帳本。
- [x] 實作電子郵件＋密碼註冊、登入、登出、忘記密碼與重設密碼流程。
- [x] 實作 Google OAuth 登入與回呼後的帳本初始化流程。
- [x] 實作雙人帳本建立、邀請連結／邀請碼與接受邀請流程。
- [x] 實作可新增、編輯、刪除的手動帳頁表單，包含金額、入納／散逸、類別、日期、備註與經手人。
- [x] 實作帳頁列表與日期區間、類別、入納／散逸、經手人篩選。
- [x] 實作月度統計、CSV 匯出與文藝色系圖表。
- [x] （已取消）實作發票號碼、日期與隨機碼的官方明細查詢入口；個人版本不依賴財政部 AppID。
- [x] （已取消）建立可選的官方載具明細查詢伺服器端介面與 AppID 設定指引；個人版本不採用此依賴。
- [x] 移除個人版本對財政部 AppID 的申請提示與官方明細查詢依賴，將照片 OCR、QR／Barcode 欄位帶入、CSV 與手動品項定位為完整的無 AppID 匯入流程。
- [x] 實作 QR Code／Barcode 文字輸入與掃描式匯入入口，將可用資料送入待確認頁。
- [x] 實作發票照片上傳、伺服器端 LLM OCR、嚴格 JSON 結構驗證與待確認頁填入流程。
- [x] 實作發票品項手動輸入與 CSV 批次匯入，並全部導向待確認頁。
- [x] 實作待確認發票頁的逐品項分類、關鍵字建議、人工修正與確認後多筆帳頁建立。
- [x] 實作帳本層級的關鍵字分類規則新增、刪除與自動優先順序；若需更換映射，可闔卷後以新類別重新添入。
- [x] 實作 IndexedDB 本機快取與離線操作佇列，支援帳頁新增、編輯與刪除。
- [x] 實作網路恢復後的補登、Timestamp LWW 伺服器端判斷、衝突診斷與同步狀態指示燈。
- [x] 撰寫 Vitest 測試，覆蓋 LWW 判斷、分類建議、CSV 格式驗證、OCR 結果驗證與關鍵帳務轉換。
- [x] 更新 `.gitignore`、撰寫完整 README、Supabase SQL 說明與 Node 伺服器部署指引；環境值透過受管理的環境設定提供。
- [x] 驗證型別檢查、測試、建置、桌面與行動版畫面，並整理可下載的專案壓縮檔。
- [x] 提供完整且不截斷的 Supabase 初始化 SQL，排除僅貼入函式尾段所造成的語法錯誤。
- [x] 在 Supabase SQL Editor 實際執行 `歲時錄-核對函式-參數型別修正版.sql`，確認 `households`、`ledger_entries`、`invoices`、`invoice_items`、`post_invoice`、`apply_ledger_mutation` 六個項目皆為「已就緒」；使用者已確認。
- [x] 建立僅清除歲時錄資料庫物件的重設初始化腳本，排除部分初始化留下的型別衝突。
- [x] 提供重設腳本並明確警示僅限尚無正式歲時錄資料時使用；使用者已回報清理成功。
- [x] 修正清理腳本的相依順序，先移除歲時錄資料表與其 RLS 規則，再移除函式和型別，避免 `is_household_member` 依賴錯誤。
- [x] 擴充入納與散逸的預設分類，涵蓋雙人生活常見情境並保留自訂分類能力。
- [x] 更新新帳本初始化函式，並提供既有帳本可重複安全執行的預設分類補齊 SQL。
- [x] 移除公開註冊與 Google 登入入口，保留固定帳號的電子郵件與密語登入及密語重設流程。
- [x] 在文件中加入 Supabase 關閉新使用者註冊、停用 Google Provider 與由管理者建立兩位帳號的設定步驟。
- [x] 驗證封閉式登入頁的桌面與行動版畫面，並以回歸測試確認密語登入與密語復原入口仍存在。
- [x] 修正「闔上帳頁」為可清除 Supabase session 並返回封閉式登入頁的實際登出操作。
- [x] 從主導航隱藏「同頁人」獨立區塊，精簡固定雙人帳本的日常操作入口，並以樣式與回歸測試避免脆弱的 DOM 操作。
- [x] 整理預設分類，移除重複或語意過近的分類，並提供既有帳本不破壞歷史帳頁的調整 SQL。
- [x] 新增可安全重複執行的帳務函式補齊 SQL，建立 `public.apply_ledger_mutation(jsonb)` 與 `public.post_invoice(uuid)`。
- [x] 驗證登出狀態重設、精簡後導航、分類與兩個帳務函式補齊腳本，並更新操作文件。
- [x] 確認 `歲時錄-整理預設分類.sql` 已在 Supabase SQL Editor 成功執行；帳務函式補齊與六個核心項目的型別核對已由使用者確認。後續發現此舊版整理腳本涵蓋不足，已由來源標記版腳本取代。
- [x] 比對補齊與核對 SQL 的函式 schema、名稱與參數簽章，修正「補齊成功但核對仍顯示缺少」的問題。
- [x] 建立獨立命名的函式核對修正版 SQL，直接交付使用者執行並確認六個項目的實際結果；使用者已確認。
- [x] 重新盤點所有預設入納與散逸分類，定義完整且保守的語意整併對照表。
- [x] 產出新版可重複執行的分類去重 SQL，將既有帳頁、發票品項與關鍵字規則安全遷移至保留分類後再刪除多餘預設分類；此初版現已由具來源保護的版本取代。
- [x] 驗證新版分類去重 SQL 的靜態對照、型別檢查、14 項測試與正式建置，並更新套用及結果核對文件；後續來源保護修正另行驗證。
- [x] （已由 V2 取代）V2 改採固定八項大目且不再有 `categories` 資料表，毋須再執行 `歲時錄-列出重複分類候選.sql`。
- [x] 為分類加入可辨識的預設／自訂來源標記，讓完整去重流程只處理已確認的歷史預設分類。
- [x] 改寫完整去重 SQL，並新增「同名同性質自訂分類不得被合併或刪除」的回歸測試。
- [x] 為新帳本初始化、完整初始化與既有標準分類補齊流程寫入穩定的 `preset_key`，並提供既有標準分類的補標 SQL。
- [x] 新增可執行的分類去重情境測試，驗證已標記歷史預設會被整併而同名同性質的未標記自訂分類會被保留。
- [x] 新增既有 21 個標準預設分類的安全補標 SQL，僅為未標記且與標準名稱、性質相符的分類補上 `current:*` 來源鍵。
- [x] 新增完整資料情境整併測試，驗證目標分類、已標記 `legacy:*` 舊分類與同名未標記自訂分類同時存在時，只遷移／刪除舊分類並保留自訂分類。
- [x] 建立獨立的分類安全整併操作清單，列出每個 SQL 檔、執行順序、需要使用者確認的地方與成功判準。
- [x] 在 README 的 Supabase 初始化段落加入醒目的操作清單連結，避免使用者必須搜尋段落文字。
- [x] 查明已確認歷史分類未被合併的實際原因：舊流程要求人工填入分類 ID 才會寫入 `legacy:*`，未填入時會成功執行但不遷移任何項目；並以「食材／食材與餐食、園藝用品／園藝與植栽」作為驗證情境。
- [x] 產出名稱式立即合併 SQL 的初版後，已撤除；該做法無法保護同名自訂分類，不再交付或建議執行。
- [x] 移除已淘汰的分類整理 SQL 與過時操作文件；目前僅保留新建初始化、資料結構與唯讀候選清單。
- [x] 以只讀候選查詢列出使用者帳本中待合併分類的 source／target category ID，要求以 ID 明確指定合併對象。
- [x] （已由 V2 取代）V2 改採固定八項大目且不再有 `categories` 資料表，毋須改寫分類 ID 修正 SQL。
- [x] （已由 V2 取代）V2 不再維護 source／target 分類整併情境，既有不安全名稱比對版本已清除。
- [x] 盤點現有 `categories`、`ledger_entries`、`invoice_items`、關鍵字規則與離線同步資料，定義「流向／大目／名目／歲時／掌簿／符契」的替換範圍。
- [x] 將流向固定為散逸與入納；將大目固定為散逸五大目（饁膳、居業、輿隸、雅趣、雜事）及入納三大目（俸祿、贏餘、奇資）。
- [x] 實作帳頁與發票品項的名目、符契與歲時欄位、可跨類別多符契關聯與 RLS 規則，並以 V2 SQL 結構斷言驗證。
- [x] 驗證添一頁新帳表單的流向、大目、名目、歲時、掌簿、符契欄位、送出 payload 與不相容流向／大目阻擋規則。
- [x] 驗證帳頁翻閱的符契篩選、符契散逸總額與流向／大目比較分析，涵蓋空結果及篩選切換的共用邏輯。
- [x] 以可執行回歸測試核對 V2 乾淨重設與唯讀核對 SQL 的資料表、RPC、RLS、符契欄位、索引與觸發器，且未對 Supabase 執行破壞性操作。
- [x] 補足 V2 離線補登 payload、表單與符契分析的回歸測試；型別檢查、20 項測試與正式建置均已通過。帳頁端對端畫面驗證待 V2 資料庫就緒後進行。
- [x] 在使用者明確確認後，已於新的 Supabase SQL Editor 執行 `歲時錄-v2-乾淨重設.sql`，再執行 `歲時錄-v2-核對.sql` 確認 V2 架構已就緒。
- [x] （已由使用者改選永久刪除）原定保留既有 Supabase 專案作為暫時備份；因免費方案上限，已改以新空白專案承載 V2 並刪除舊專案。
- [x] 在新的 Supabase 專案建立後，已取得 Project URL 與 Publishable Key，並以受管理的環境設定更新網站前端連線；身份服務連線、20 項回歸測試、型別檢查與正式建置均已通過。
- [x] 已在新的 Supabase 專案一次套用 `歲時錄-v2-乾淨重設.sql`，再執行 `歲時錄-v2-核對.sql`；使用者確認 V2 結構、RPC、RLS、索引與觸發器 12 項均已就緒。
- [x] 在新的 Supabase 專案關閉公開註冊、停用 Google Provider，並由管理者建立固定登入帳號；使用者已確認。
- [x] 後續已以固定帳號完成封閉登入、共用帳本、添新帳、符契與憑據入冊的程式／實機驗證；離線補登的完整實機流程改由現行清單持續追蹤。
- [x] 將 V1 分類去重與舊專案 V2 重設待辦標示為已由新專案 V2 初始化取代，並保留歷程說明。
- [x] 依使用者明確授權，已永久刪除舊的「歲時錄 · Vellum Tides」Supabase 專案；其資料庫、Auth 帳號與既有 API 設定均不再保留。
- [x] 使用者已自行在 Supabase 建立固定登入帳號；後續不代理操作或接管使用者瀏覽器，僅依其回報進行網站端驗證與支援。
- [x] （歷程）已完成 UID 對應與名稱設定；目前名稱已由後續需求改為玉瑟／石琴。
- [x] 提供固定帳號名稱設定與唯讀核對 SQL，並確認網站帳頁及掌簿欄位依 UID 顯示對應名稱。
- [x] （歷程）已更正固定 UID 對應，後續名稱已由使用者指定改為玉瑟／石琴。
- [x] 提供僅更新既有 `household_members` 顯示名稱的修復 SQL；最新名稱更新改由「石琴玉瑟與讀取權限修復」腳本統一處理。
- [x] （介面已由固定共用帳本取代）不再需要建立帳本或接受邀請；實際共用資料仍待固定共用帳本 SQL 套用後，以兩位帳號驗證。
- [x] `ensure_personal_household` 與 `accept_household_invite` 已改由兩位帳號的 `raw_user_meta_data.display_name` 自動帶入預設別名。
- [x] （已由固定共用帳本取代）先前曾提供 `/共同帳本` 邀請介面，現在已自主路由移除。
- [x] 網站端已將新帳頁的「掌簿」限制為固定兩名，依登入 UID 自動帶入對應掌簿，且不提供自行輸入、改名或新增掌簿名稱的介面。
- [x] 已在新的 Supabase 專案執行 `歲時錄-v2-固定掌簿修復.sql`；使用者確認成功，資料庫層亦強制以目前登入 UID 寫入掌簿，防止繞過網站介面的自行修改。
- [x] 已將新帳頁的掌簿欄位精簡為只顯示登入 UID 對應的名稱，移除冗長說明文字且維持不可修改；名稱現為玉瑟／石琴。
- [x] 已將固定掌簿的顯示名稱改為「玉瑟／石琴」，並同步更新前端、Supabase 修復 SQL、核對資料與 27 項回歸測試。
- [x] 已在 V2 初始化與名稱修復腳本補齊 authenticated 對受 RLS 保護資料表的必要讀取權限，修正前端帳頁與共同帳本讀取遭拒的問題。
- [x] 使用者已在 Supabase SQL Editor 執行 `歲時錄-v2-石琴玉瑟與讀取權限修復.sql`，並回報核對皆已就緒，完成現有資料與權限的套用驗證。
- [x] 修正 Android 與 iOS 裝置在新帳頁輸入資料時，每次按鍵都會讓輸入法關閉並失去欄位焦點的問題；已固定欄位元件識別並以 28 項測試、型別檢查、正式建置及行動尺寸畫面驗證。
- [x] iOS 與 Android 均由使用者回報連續輸入金額與文字正常，確認不再發生失焦或鍵盤收起。
- [x] 在「流向」加入「月痕對照」、「散逸羅盤」與「符契脈絡」三種整月比較圖表，並提供顯示內容設定。
- [x] 在「設定」加入流向洞察顯示選項，讓使用者決定要呈現哪些比較圖表；選項會儲存在該裝置的瀏覽器中。
- [x] 讓新帳頁的「符契」可快速選用既有標籤，無須重複手動輸入。
- [x] 修正帳頁使用過程中的非預期畫面閃爍：帳本初始化會依登入者只執行一次，後續資料刷新不再重設整頁載入狀態。
- [x] 將網站介面簡化為玉瑟與石琴固定共用同一份帳頁資料，移除帳本建立、邀請碼與加入帳本的日常引導。
- [x] 後續固定共用帳本與玉瑟／石琴實際登入、讀寫及憑據歸帳均已由使用者確認；原本要求文字核對結果的歷程項目結案。
- [x] 已將固定共用帳本介面、預設資料與 SQL 腳本中的暫用帳本名稱統一改回「歲時錄 · Vellum Tides」。
- [x] 登入頁可見標題已完整呈現「歲時錄 · Vellum Tides」，並加入可存取標籤；資料庫與所有帳本建立、解析流程均使用完整名稱。
- [x] 已修正主帳頁初始化呼叫仍使用舊暫用帳本名稱的遺漏，所有建立與解析流程皆使用「歲時錄 · Vellum Tides」。
- [x] 在現行 V2 介面恢復發票照片上傳 OCR、QR／Barcode 或文字帶入、待確認與逐品項歸類後寫入帳頁的流程。
- [x] 為單一共用帳本與發票 OCR 匯入流程新增回歸測試，並完成 29 項測試、型別檢查、正式建置與桌面／行動登入版面驗證。
- [x] 已驗證並完成「待確認」整合為「憑據入冊」第二步：待續編清單會載入既有憑據與品項、完成校對可歸帳，且不再存在獨立導覽頁。
- [x] 已新增憑據入冊續編工作流回歸測試，覆蓋「待續編清單 → 載入第二步 → 校對 → 歸帳」的完整程式契約。
- [x] 已移除與「本月符契」重疊的「符契脈絡」月度洞察，流向頁僅保留具辨識價值的本月符契比較。
- [x] 已移除側欄重複的同步狀態，改於頁首保留單一且可理解的同步指示。
- [x] 已修正固定共用帳本下憑據入冊暫存時 `invoices` 寫入遭拒的 RLS／授權問題，並一併補齊 `invoice_items` 寫入與待確認續編讀取的最小權限；30 項測試、型別檢查與正式建置通過。
- [x] 使用者已執行 `歲時錄-v2-憑據入冊權限修復.sql`，並回報三項唯讀核對皆為 `true`：可暫存憑據、可寫入品項、可校對品項。
- [x] 使用者已以固定帳號完成憑據暫存、進入第二步、校對品項並正確入帳的實機驗證。
- [x] 將帳頁翻閱重構為先依月份成冊、再依日期將當日帳頁框組在一起的層級式版面，保留既有篩選、符契比較、匯出與帳頁覆寫／收起操作；新增回歸測試，完成 32 項測試、型別檢查與正式建置。
- [x] 已改為適合大量資料的精簡摘要與 45 筆分段載入；實際資料庫 RPC 啟用狀態已移至現行清單追蹤。
- [x] 已新增完整 JSON 典藏、格式與帳本核對、預覽及僅追加式還原；實際資料庫 RPC 啟用狀態已移至現行清單追蹤。
- [x] 已清理確認未使用、重複或已淘汰的程式、SQL 與說明檔，保留現行 V2 所需內容。
- [x] 已依固定共用帳本、玉瑟與石琴及 V2 詞彙整編 todo.md；歷程維持隱藏註解以供追溯。

-->
